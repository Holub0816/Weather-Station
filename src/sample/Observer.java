package sample;

/**
 * An interface that allows for updating information about observers.
 */
public interface Observer {
    /**
     * Actualize information for observer.
     */
    void update();
}
